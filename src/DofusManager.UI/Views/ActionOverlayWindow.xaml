<Window x:Class="DofusManager.UI.Views.ActionOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mi="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual">

    <Window.Resources>
        <Style x:Key="OverlayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#44FFFFFF" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#66FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="#CC1A1A1A"
            BorderBrush="#555" BorderThickness="1" Padding="4">
        <StackPanel>
            <!-- Grip de drag -->
            <Border Background="#44FFFFFF" CornerRadius="4" Height="8"
                    Margin="8,2,8,4" Cursor="SizeAll"
                    MouseLeftButtonDown="Grip_MouseLeftButtonDown" />

            <!-- Capture d'ame toggle -->
            <Button Command="{Binding ToggleCaptureAmeOverlayCommand}"
                    ToolTip="Rappel Capture d'ame"
                    Style="{StaticResource OverlayButtonStyle}">
                <mi:MaterialIcon Kind="Sword" Width="22" Height="22" />
            </Button>

            <!-- Invite groupe -->
            <Button Command="{Binding InviteAllCommand}"
                    ToolTip="Inviter le groupe"
                    Style="{StaticResource OverlayButtonStyle}">
                <mi:MaterialIcon Kind="AccountGroup" Width="22" Height="22" />
            </Button>

            <!-- AutoFollow -->
            <Button Command="{Binding ToggleAutoFollowCommand}"
                    ToolTip="Toggle autofollow"
                    Style="{StaticResource OverlayButtonStyle}">
                <mi:MaterialIcon Kind="Walk" Width="22" Height="22" />
            </Button>

            <!-- Havre-Sac -->
            <Button Command="{Binding SendAllToHavreSacCommand}"
                    ToolTip="Envoyer au havre-sac"
                    Style="{StaticResource OverlayButtonStyle}">
                <mi:MaterialIcon Kind="Home" Width="22" Height="22" />
            </Button>
        </StackPanel>
    </Border>
</Window>
