<UserControl x:Class="DofusManager.UI.Views.HotkeyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Barre d'actions -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="{Binding HotkeysButtonText}"
                    Command="{Binding ToggleHotkeysCommand}"
                    Padding="10,5" Margin="0,0,5,0" />
            <Button Content="Définir leader"
                    Command="{Binding SetLeaderCommand}"
                    Padding="10,5" />
        </StackPanel>

        <!-- Liste des slots -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Slots}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectedIndex="{Binding SelectedLeaderIndex, Mode=TwoWay}"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Slot" Binding="{Binding SlotIndex}" Width="50" />
                <DataGridTextColumn Header="Touche" Binding="{Binding HotkeyLabel}" Width="70" />
                <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="70" />
                <DataGridTextColumn Header="Titre" Binding="{Binding Title}" Width="*" />
                <DataGridCheckBoxColumn Header="Leader" Binding="{Binding IsLeader, Mode=OneWay}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Raccourcis spéciaux -->
        <GroupBox Grid.Row="2" Header="Raccourcis de navigation" Margin="0,10,0,0" Padding="5">
            <UniformGrid Columns="2" Rows="2">
                <TextBlock Text="Suivant : Ctrl+Tab" Margin="5" />
                <TextBlock Text="Précédent : Ctrl+Shift+Tab" Margin="5" />
                <TextBlock Text="Dernière fenêtre : Ctrl+`" Margin="5" />
                <TextBlock Text="Panic leader : Ctrl+F1" Margin="5" />
            </UniformGrid>
        </GroupBox>

        <!-- Statut -->
        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem Content="{Binding StatusText}" />
        </StatusBar>
    </Grid>
</UserControl>
