<Window x:Class="DofusManager.UI.Views.ZaapPickerOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mi="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Height="400"
        Width="400"
        WindowStartupLocation="Manual">

    <Window.Resources>
        <!-- Style pour les items de la liste -->
        <Style x:Key="ZaapItemButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#55FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour le bouton favori -->
        <Style x:Key="FavoriteButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="#CC1A1A1A"
            BorderBrush="#555" BorderThickness="1" Padding="8">
        <DockPanel>
            <!-- Header : recherche + bouton fermer -->
            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,6">
                <Button DockPanel.Dock="Right" Click="CloseButton_Click"
                        ToolTip="Fermer" Focusable="False" Cursor="Hand"
                        Background="Transparent" BorderThickness="0" Padding="4" Margin="4,0,0,0"
                        VerticalAlignment="Center">
                    <mi:MaterialIcon Kind="Close" Width="14" Height="14" Foreground="#999" />
                </Button>
                <Border Background="#33FFFFFF" CornerRadius="4" Padding="6,4">
                    <DockPanel>
                        <mi:MaterialIcon Kind="Magnify" Width="16" Height="16" Foreground="#999"
                                         DockPanel.Dock="Left" Margin="0,0,4,0" VerticalAlignment="Center" />
                        <TextBox x:Name="SearchBox"
                             Text="{Binding ZaapFilterText, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent"
                             Foreground="White"
                             CaretBrush="White"
                             BorderThickness="0"
                             VerticalAlignment="Center"
                             FontSize="13" />
                    </DockPanel>
                </Border>
            </DockPanel>

            <!-- Liste des territoires -->
            <ListBox ItemsSource="{Binding FilteredTerritories}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingPanel.IsVirtualizing="True">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <!-- Bouton favori -->
                            <Button DockPanel.Dock="Right"
                                    Style="{StaticResource FavoriteButtonStyle}"
                                    Command="{Binding DataContext.ToggleFavoriteZaapCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding Name}"
                                    ToolTip="Favori">
                                <mi:MaterialIcon Width="14" Height="14">
                                    <mi:MaterialIcon.Style>
                                        <Style TargetType="mi:MaterialIcon">
                                            <Setter Property="Kind" Value="StarOutline" />
                                            <Setter Property="Foreground" Value="#666" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                    <Setter Property="Kind" Value="Star" />
                                                    <Setter Property="Foreground" Value="#FFD700" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </mi:MaterialIcon.Style>
                                </mi:MaterialIcon>
                            </Button>

                            <!-- Bouton destination (clic = voyage) -->
                            <Button Style="{StaticResource ZaapItemButtonStyle}"
                                    Command="{Binding DataContext.TravelToZaapCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding Name}">
                                <DockPanel>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding Coordinates}"
                                               Foreground="#888" FontSize="11" VerticalAlignment="Center"
                                               Margin="6,0,0,0" />
                                    <TextBlock Text="{Binding DisplayName}" FontSize="12"
                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                </DockPanel>
                            </Button>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Border>
</Window>
